import { cva } from "class-variance-authority"


export const segmentedProgressBarVariants = cva(
    [],
    {
      variants: {},
      defaultVariants: {},
    }
  )

export default segmentedProgressBarVariants